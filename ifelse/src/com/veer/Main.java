package com.veer;

public class Main {

    public static void main(String[] args) {
        boolean gameOver = true ;
        int score =800;
        int levelcompleted = 5;
        int bonus =100;

        if (gameOver = true)
        {
           int finalscore = score + (levelcompleted * bonus);
           System.out.println("hello final score was " + finalscore);
        }

        if (gameOver = true)
        {
            int finalscore  = (score  + 9200) + ((levelcompleted + 3) * (bonus *2));
            System.out.println("hello final score was " + finalscore);
        }
    }
}
